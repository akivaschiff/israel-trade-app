{
  "name": "trade-chat-app",
  "version": "1.0.0",
  "private": true,
  "description": "Israeli trade data chat application",
  "scripts": {
    "install:backend": "cd backend && npm install",
    "install:frontend": "npm install",
    "build": "npm run build:frontend",
    "build:frontend": "vite build",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
